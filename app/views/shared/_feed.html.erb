<% if @feed_items.any? %>
    <ol class="microposts">
        <%= render @feed_items %>
    </ol>
    <%== render partial: 'shared/bootstrap_nav', locals: {pagy: @pagy} if @pagy.pages > 1 %>
<% end %>
